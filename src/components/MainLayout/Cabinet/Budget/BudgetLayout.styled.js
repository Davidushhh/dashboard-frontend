import styled from "@emotion/styled";

import { Box } from "@mui/material";

export const BudgetWidgetWrapper = styled(Box)`
  open-budget-widget:first-child #open-budget-widget {
    margin: 0;
  }
`;
